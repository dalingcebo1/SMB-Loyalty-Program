# Top-level (unpinned) dependencies for the backend.
# Run pip-compile (if adopted) or manual freeze to update requirements.txt.
alembic  # Migration tool
fastapi==0.116.1  # Pin FastAPI to align with tested version
itsdangerous
passlib[bcrypt]
# Temporary pin: passlib 1.7.4 expects bcrypt < 4 (uses deprecated __about__ attr)
bcrypt==3.2.2
pydantic==2.12.0  # Match requirements.txt pinned
pydantic-settings==2.10.1
email-validator>=2
psutil
python-dotenv
python-jose
python-multipart
psycopg2-binary
qrcode[pil]
requests
slowapi
sendgrid
sqlalchemy==2.0.43
uvicorn[standard]==0.35.0  # Ensure matching server version to avoid upstream auto-upgrades
PyJWT
firebase-admin
Pillow
sentry-sdk>=1.45.1
redis

# Security: Ensure non-vulnerable setuptools (used during installs/builds)
setuptools>=78.1.1
