import React from 'react';

// Legacy subscription management page placeholder.
// Original implementation removed because supporting subscription feature modules
// are not present in this commit snapshot. This stub prevents broken imports and
// JSX parse errors during type checking.

const SubscriptionManagePage: React.FC = () => {
  return (
    <div style={{padding: '2rem'}}>
      <h1 className="text-xl font-semibold">Subscription Management (Legacy)</h1>
      <p className="text-sm text-gray-600 mt-2">This legacy subscription admin page has been temporarily disabled. Supporting feature modules are absent in the current code state.</p>
    </div>
  );
};

export default SubscriptionManagePage;
