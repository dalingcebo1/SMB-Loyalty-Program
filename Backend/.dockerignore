__pycache__/
*.pyc
*.pyo
*.pyd
*.sqlite3
*.db
.devcontainer/
.git/
.gitignore
Dockerfile
*.egg-info
.env
*.env
venv/
.venv/
/tests/
.mypy_cache/
.pytest_cache/
.coverage
htmlcov/
.dist/
build/
pytest.ini
# IMPORTANT: We need Alembic config inside the image to run migrations in Azure Container Apps
# Do NOT ignore alembic.ini; it must be copied by the Dockerfile
# (If you ever need to exclude it locally, comment this back in, but CI/CD expects it present.)
