"""Vertical plugin stubs

Each vertical can extend shared hooks for domain specific behavior.
The central dispatch (vertical_dispatch.py) calls the first non-None
handler implementation across registered vertical plugins.
"""
